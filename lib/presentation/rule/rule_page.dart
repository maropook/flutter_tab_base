import 'package:flutter/material.dart';

class RulePage extends StatelessWidget {
  const RulePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('利用規約画面'),
      ),
      body: const Center(
        child: Text('利用規約画面'),
      ),
    );
  }
}
